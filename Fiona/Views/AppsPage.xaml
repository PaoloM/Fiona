<Page
    x:Class="Fiona.Views.AppsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Fiona.Core.Models"
    xmlns:helpers="using:Fiona.Helpers"
    xmlns:viewmodels="using:Fiona.ViewModels"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:AppsViewModel x:Name="BaseVM"/>
    </Page.DataContext>

    <StackPanel Orientation="Vertical">
        <TextBlock Text="{x:Bind ViewModel.CurrentAppletName}"></TextBlock>

        <StackPanel Visibility="{x:Bind ViewModel.PlaylistCommandsVisibility}" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Left">
            <Button Background="{x:Null}" Command="{x:Bind ViewModel.PlayPlaylistCommand}" Margin="4,0,4,0">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE102;" FontSize="{StaticResource SmallGlyphSize}" Margin="0,0,8,0"/>
                    <TextBlock x:Uid="Playlist_PlayAll" FontSize="{StaticResource SmallFontSize}"/>
                </StackPanel>
            </Button>
            <Button Background="{x:Null}" Command="{x:Bind ViewModel.QueuePlaylistCommand}" Margin="4,0,4,0">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE109;" FontSize="{StaticResource SmallGlyphSize}" Margin="0,0,8,0"/>
                    <TextBlock x:Uid="Playlist_AddToQueue" FontSize="{StaticResource SmallFontSize}"/>
                </StackPanel>
            </Button>
        </StackPanel>
        
        <Grid x:Name="ContentArea">
        <controls:AdaptiveGridView                      
            IsItemClickEnabled="True"
            ItemsSource="{x:Bind BaseVM.Apps.Applets, Mode=OneWay}"
            SelectionMode="None"            
            ItemClickCommand="{x:Bind BaseVM.AppSelectedCommand}"
            Margin="44,0,36,36" >
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:Applet">
                    <Grid x:Name="itemThumbnail" Width="160" Height="196" Margin="8,8,8,8"
                        Background="Transparent"
                        helpers:VisibilityHelper.VisibilityOnHover="{Binding ElementName=ItemControls}">
                        <!--Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">-->
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
                            <Image Source="{x:Bind GetIconUrl, Mode=OneWay}" Width="160" Height="160" Stretch="UniformToFill"/>
                            <TextBlock HorizontalAlignment="Center"
                                Margin="{StaticResource XXSmallTopMargin}"
                                Style="{ThemeResource CaptionTextBlockStyle}"
                                Text="{x:Bind Text}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                        </StackPanel>
                        <StackPanel x:Name="ItemControls" Orientation="Horizontal"
                                    HorizontalAlignment="Center" VerticalAlignment="Top"
                                    Visibility="Collapsed" Margin="0,64,0,0">
                            <Button x:Name="PlayAlbum" Width="32" Height="32" CornerRadius="16" Margin="4,0,4,0"
                                    >
                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE102;" FontSize="14"/>
                            </Button>
                            <Button x:Name="QueueAlbum" Width="32" Height="32" CornerRadius="16" Margin="4,0,4,0"
                                    >
                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE109;" FontSize="14"/>
                            </Button>
                        </StackPanel>

                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>

        </controls:AdaptiveGridView>
        
    </Grid>
    </StackPanel>
</Page>
