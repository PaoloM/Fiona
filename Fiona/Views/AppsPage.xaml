<Page
    x:Class="Fiona.Views.AppsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Fiona.Core.Models"
    xmlns:helpers="using:Fiona.Helpers"
    xmlns:viewmodels="using:Fiona.ViewModels"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:AppsViewModel x:Name="BaseVM"/>
    </Page.DataContext>
    
    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">
        <Button Command="{x:Bind BaseVM.AppletNavigationBackCommand}" Width="44" Height="32" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,-30,0,0">
            <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE0A6;" />
        </Button>
        <controls:AdaptiveGridView                      
            IsItemClickEnabled="True"
            ItemsSource="{x:Bind BaseVM.Apps.Applets, Mode=OneWay}"
            SelectionMode="None"            
            ItemClickCommand="{x:Bind BaseVM.AppSelectedCommand}">
            <GridView.ItemContainerStyle>
                <Style TargetType="FrameworkElement">
                    <Setter Property="Margin" Value="0 0 20 20"/>
                </Style>
            </GridView.ItemContainerStyle>
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:Applet">
                    <Grid x:Name="itemThumbnail" Width="160" Height="215" >
                        <!--Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">-->
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
                            <Image Source="{x:Bind GetIconUrl, Mode=OneWay}" Width="160" Height="160" Stretch="UniformToFill"/>
                            <TextBlock HorizontalAlignment="Center"
                                Margin="{StaticResource XXSmallTopMargin}"
                                Style="{ThemeResource CaptionTextBlockStyle}"
                                Text="{x:Bind Text}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>

        </controls:AdaptiveGridView>
        
    </Grid>
</Page>
